// var utils = require('../utils')

// describe('failsafe redux-app', function () {
//   // build the app
//   beforeEach(utils.verifyNoBrowserErrors)

//   it('should have no errors - click', function (done) {
//     browser.url('/react/react/index.html')//wait(until.elementLocated(By.id('incr')))

//     browser.executeAsync(
//       function(cb) {
//         document.getElementById('incr').click()
//         cb()
//       }
//     ).then(function (response) {
//         done()
//       }, function (error) {
//         browser.log(error)
//       })
//   })

//   afterEach(utils.verifyNoBrowserErrors)
// })
